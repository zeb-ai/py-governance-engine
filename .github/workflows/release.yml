name: Build and Release Executables

on:
  push:
    tags:
      - "v*"

# Opt into Node 24 now so deprecated-action warnings stop blocking you
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            artifact_name: z-grc-proxy-macos-arm64
            ext: ""
          - os: ubuntu-latest
            artifact_name: z-grc-proxy-linux-x86_64
            ext: ""
          - os: ubuntu-24.04-arm
            artifact_name: z-grc-proxy-linux-arm64
            ext: ""
          - os: windows-latest
            artifact_name: z-grc-proxy-windows-x64
            ext: ".exe"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --all-groups --all-extras

      - name: Build executable (one-dir)
        run: uv run pyinstaller z-grc-proxy.spec

      # PyInstaller one-dir produces dist/z-grc-proxy/  (a folder)
      # Rename the folder, then zip it into a single distributable.
      - name: Package one-dir bundle (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mv dist/z-grc-proxy "dist/${{ matrix.artifact_name }}"
          cd dist
          tar -czvf "${{ matrix.artifact_name }}.tar.gz" "${{ matrix.artifact_name }}"

      - name: Package one-dir bundle (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Rename-Item -Path "dist/z-grc-proxy" -NewName "${{ matrix.artifact_name }}"
          Compress-Archive -Path "dist/${{ matrix.artifact_name }}" `
                           -DestinationPath "dist/${{ matrix.artifact_name }}.zip"

      - name: Upload artifact (Linux/macOS)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}.tar.gz
          if-no-files-found: error

      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}.zip
          if-no-files-found: error

  release:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          merge-multiple: true

      - name: List downloaded files (debug)
        run: ls -lah ./artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
          files: ./artifacts/**/*
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}